// Import stylesheets
import '../style.css';

// TypeScript code
const title: string = 'Implementations of the QuickSort Algorithm and using the Bridge Pattern with TypeScript';
const appDiv: HTMLElement = <HTMLElement> document.getElementById('app');

document.title = title;
appDiv.innerHTML = `<h1>${title}</h1>`;
